To run script: `python3 ./build_json.py`
Then, `python3 ./build_graph.py`
Then, to read the graph and the json file, run: `python3 read_graph.py` and you have two variables: `json_graph` and `graph`, you can play with them.